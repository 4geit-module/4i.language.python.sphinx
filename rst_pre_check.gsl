.template 0

parentnode = projectnode.parent()

if defined(parentnode) & name(parentnode) = 'dir'
  if defined(parentnode.path)
    define projectnode.path = parentnode.path + '/'
    define projectnode.source_path = parentnode.source_path + '/'
  endif
else
  define projectnode.path = 'source/'
  define projectnode.source_path = ''
endif

.endtemplate